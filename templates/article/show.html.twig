{% extends 'base.html.twig' %}



{% block body %}

    <h1>{{ article.title }}</h1>
    <p>Date de publication : {{ article.date_de_publication }}</p>

    <p>{{ article.content }}</p>

{% endblock %}
